<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyToast">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">bold</item> only bold!
        <item name="android:fontFamily">sans-serif</item> For custom fonts just add the path -> fonts/myfont.ttf
        <item name="android:colorBackground">@color/yellow</item>

        <item name="android:alpha">3</item>   value between 0-255 where 255 is full solid
        <item name="android:icon">@drawable/home</item>

    </style>

</resources>
